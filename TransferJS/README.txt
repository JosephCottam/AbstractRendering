You must run this example through a web-server, just browsing to the index.html
file will not work.  

python -m SimpleHTTPServer
Execute from the same directory as index.html Will provide a simple webserver.
Then navigate to http://localhost:8000
