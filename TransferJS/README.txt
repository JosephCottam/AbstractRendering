You must run this example through a web-server, just browsing to the index.html
file will not work.  

Execute in this directory:
  * Python 2.x: python -m SimpleHTTPServer
  * Python 3.x: python -m http.server 8080

Open a browser  http://localhost:8000 and an image should appear!
